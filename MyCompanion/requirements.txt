# MyCompanion Framework - AI Desktop Companion
# Python dependencies (Optimized for AMD GPU / CPU-only, No CUDA)

# Core Async & Event Loop
qasync>=0.27.1
asyncio-throttle>=1.0.2

# GUI Framework (PyQt6 for Desktop Widget)
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0

# Web Server & API (FastAPI for Dashboard)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-socketio>=5.11.0
aiohttp>=3.9.0
websockets>=12.0

# HTTP Client for Cloud APIs
httpx>=0.26.0
openai>=1.10.0  # For OpenAI-compatible endpoints (Groq, etc.)
anthropic>=0.18.0  # Optional, for Claude

# Speech-to-Text (Cloud-based to save CPU)
# Using OpenAI Whisper API via httpx (no local installation needed)

# Text-to-Speech (Local, lightweight)
edge-tts>=6.1.9  # Microsoft Edge TTS - Free, high quality, supports many voices
pygame>=2.5.2  # For audio playback

# Image Processing (Lightweight for screen capture)
Pillow>=10.2.0
mss>=9.0.1  # Fast screen capture

# Memory & Storage
sqlite3  # Built-in
aiosqlite>=0.19.0  # Async SQLite

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
tenacity>=8.2.3  # Retry logic for API calls

# Automation (Keyboard/Mouse control)
pyautogui>=0.9.54
pynput>=1.7.6  # Alternative for keyboard/mouse control

# Note: No torch/tensorflow required - all AI inference is cloud-based
# This ensures compatibility with AMD GPUs and low-spec hardware
